# GrabTicket 기획 및 설계서

## 1. 문서 목적

본 문서는 현재 `GrabTicket` 프론트엔드 프로젝트에 반영된 기능을 기준으로 서비스 기획, 화면 구조, 기능 규칙, 데이터 모델, 인증 구조, 얼굴 등록 및 입장 검증 흐름을 정리한 기준 문서다.

이 문서는 다음 목적을 가진다.

- 현재까지 구현된 기능 범위를 한 번에 파악한다.
- 역할별 사용 흐름과 제약 조건을 명확히 정의한다.
- 이후 백엔드/API/DB 설계 및 운영 정책 확장 시 기준 자료로 활용한다.

## 2. 서비스 개요

GrabTicket은 공연 입장 운영을 위한 역할 기반 웹 앱이다. 하나의 앱 안에서 세 가지 운영 모드를 제공한다.

- 이용자 앱
- 공연장 테블릿
- 테블릿 관리자

핵심 목표는 다음과 같다.

- 이용자는 회원가입, 로그인, 얼굴 등록, 티켓 구매, 내 티켓 조회를 수행한다.
- 공연장 테블릿은 QR 스캔과 얼굴 인증을 통해 입장을 검증한다.
- 테블릿 관리자는 회원 목록, 공연 등록, 티켓 등록 현황, 입장 로그를 관리한다.

## 3. 사용자 역할 정의

### 3.1 이용자

- 회원가입 및 로그인 가능
- 본인 얼굴 등록 가능
- 얼굴 등록 완료 후 티켓 구매 가능
- 본인 프로필 조회, 비밀번호 변경, 로그아웃 가능
- 본인이 구매한 티켓 조회 및 취소 가능

### 3.2 공연장 테블릿

- 별도 회원가입 없음
- 백엔드 시드 계정(`role=venue-tablet`)으로 로그인 (JWT)
- QR 스캔 화면 진입 가능
- 얼굴 인증 화면 진입 가능
- 프로필 조회, 로그아웃 가능 (비밀번호 변경은 Django admin에서 처리)

로그인 계정은 백엔드 데이터 마이그레이션으로 시드되며, 자격증명은 프론트 코드에
하드코딩하지 않는다. 개발 기본 시드 계정 정보는 백엔드 저장소를 참고한다.

### 3.3 테블릿 관리자

- 별도 회원가입 없음
- 백엔드 시드 계정(`role=tablet-admin`)으로 로그인 (JWT)
- 회원목록, 공연등록, 티겟등록, 입장로그 확인 가능
- 프로필 조회, 로그아웃 가능 (비밀번호 변경은 Django admin에서 처리)

로그인 계정은 백엔드 데이터 마이그레이션으로 시드되며, 자격증명은 프론트 코드에
하드코딩하지 않는다. 개발 기본 시드 계정 정보는 백엔드 저장소를 참고한다.

## 4. 정보 구조

## 4.1 메인 허브

앱 진입 시 메인 홈에서는 세 가지 메뉴만 노출한다.

- 이용자
- 공연장 테블릿
- 테블릿 관리자

상단 바는 현재 역할 상태와 `Home` 버튼, 앱 버전을 표시한다.

## 4.2 역할별 탭 구조

### 이용자

- 얼굴등록
- 티켓구매
- 프로필

### 공연장 테블릿

- QR스캔
- 얼굴 인증
- 프로필

### 테블릿 관리자

- 회원목록
- 공연등록
- 티겟등록
- 입장로그
- 프로필

## 5. 인증 및 세션 설계

## 5.1 저장 방식

현재 인증 및 데이터 저장은 브라우저 `localStorage` 기반이다.

- 사용자 계정, 얼굴 프로필, 공연, 티켓, 입장 로그 등을 로컬에 저장
- 역할별 세션도 `localStorage`에 저장
- 일반 브라우저와 시크릿 브라우저는 저장소가 분리됨

## 5.2 이용자 인증 흐름

### 회원가입

이용자 최초 진입 시 회원가입 가능

입력 항목:

- 이메일
- 이름
- 전화번호
- 비밀번호
- 비밀번호 확인

규칙:

- 비밀번호는 2회 입력해야 함
- 비밀번호와 비밀번호 확인이 일치해야 가입 가능
- 가입 후 자동 로그인 처리 가능

### 로그인

이미 가입된 이용자는 이메일/비밀번호로 로그인한다.

동작 규칙:

- 로그인 성공 시 이용자 세션 저장
- 이후 재진입 시 자동 로그인 유지
- 로그인 성공 후 이용자 탭의 가장 왼쪽 화면으로 이동
- 현재 구조상 가장 왼쪽은 `얼굴등록`

### 로그아웃

- 프로필 화면에서 로그아웃 가능
- 로그아웃 시 세션 삭제
- 로그아웃 후 이용자의 가장 왼쪽 탭 화면으로 이동하고 로그인 화면 노출

## 5.3 공연장 테블릿 / 관리자 인증 흐름

- 이메일 + 비밀번호 로그인
- 별도 회원가입 없음
- 로그인 성공 시 역할 세션 저장
- 자동 로그인 유지
- 로그인 후 역할 탭의 가장 왼쪽 화면으로 이동

왼쪽 첫 화면 기준:

- 공연장 테블릿: `QR스캔`
- 관리자: `회원목록`

로그아웃 시:

- 세션 삭제
- 같은 역할의 왼쪽 첫 탭 화면으로 이동
- 로그인 화면 다시 노출

## 6. 이용자 기능 설계

## 6.1 얼굴등록

### 목적

이용자 본인의 얼굴 이미지를 등록하여 향후 공연장 입장 시 얼굴 인증 기반 검증에 활용한다.

### 촬영 구조

현재 얼굴등록은 총 7장 촬영 구조다.

촬영 순서:

1. 정면
2. 오른쪽 코
3. 오른쪽 볼
4. 왼쪽 코
5. 왼쪽 볼
6. 턱 올림
7. 턱 내림

### 촬영 인터랙션

- `준비` 버튼으로 카메라 시작
- `자동촬영` 버튼은 라이브니스 조건 충족 시 활성화
- `중단` 버튼으로 촬영 중지
- 자동 촬영 중 카운트다운과 촬영 안내가 오버레이로 표시
- 음성 안내 문구를 화면에도 표시
- 촬영 완료 후 각 장면별 재촬영 가능
- 전체 장수 충족 시 최종 저장 가능
- 현재 구현상 자동촬영 종료 후 얼굴 프로필 저장도 수행

### 라이브니스 및 품질 체크

`@mediapipe/tasks-vision`의 `FaceLandmarker`를 사용한다.

현재 확인 항목:

- 얼굴 감지
- 한 명만 인식
- 중앙 정렬
- 적정 크기
- 조명 적정
- 얼굴 가림 없음
- 정면
- 오른쪽 회전
- 왼쪽 회전
- 눈 깜빡임

점수 설계:

- 품질/동작 진행도를 기반으로 점수 계산
- 소수점 둘째 자리까지 표시
- 자동촬영 활성화는 현재 점수 기준으로 동작
- 현재 기준: `80점 이상`이며 최소 감지 조건 충족 시 자동촬영 활성화

시각적 피드백:

- 각 조건은 3단계 배경색으로 표시
- 낮음: 살구색
- 중간: 노란색
- 높음: 녹색

### 얼굴 등록 완료 조건

- 7장 모두 촬영 완료
- 각 이미지 blob 저장 완료
- 얼굴 프로필 생성 성공

저장 결과:

- `FaceProfile.status = ready`
- `snapshotBlobIds`에 7개 이미지 참조 저장

## 6.2 티켓구매

### 사전 조건

- 이용자 로그인 완료
- 얼굴등록 완료

얼굴 프로필이 없거나 `ready` 상태가 아니면 구매 대신 얼굴등록 유도 메시지를 표시한다.

### 구매 흐름

입력 순서:

1. 공연선택
2. 공연날짜 선택
3. 공연회차 선택
4. 구매 수량 선택

구매자 선택은 없다. 현재 로그인 계정을 구매자로 자동 연결한다.

### 구매 규칙

- 동일 공연 회차 기준 좌석은 `1, 2, 3 ...` 순으로 자동 배정
- 이미 구매된 좌석은 다시 배정하지 않음
- 동일 계정은 동일 공연 회차에서 최대 3장까지 구매 가능
- 구매 수량은 가능한 범위 내에서 `1`, `2`, `3` 선택
- 남은 좌석보다 많이 구매할 수 없음
- 취소된 티켓은 좌석 점유와 구매 제한 계산에서 제외

### 생성 정보

티켓 생성 시 포함 정보:

- 고유 티켓 번호
- 공연 ID
- 공연명
- 공연일시
- 회차
- 좌석번호
- 구매자 ID
- 소지자 ID
- 상태
- 생성일시

### 내 티켓 조회

티켓구매 화면 하단에서 현재 로그인 사용자의 구매 티켓을 확인할 수 있다.

표시 정보:

- 공연명
- 티켓번호
- 좌석 / 상태
- 공연날짜: `YYYY. M. D. HH:mm / N회차`
- QR 코드

### 티켓 취소

- 내 티켓에서 언제든 취소 가능
- 취소 전 커스텀 팝업 표시
- 문구: `정말 취소하시겠습니까?`
- 확인 후 상태를 `cancelled`로 변경

## 6.3 프로필

이용자 프로필에서 확인/수정 가능한 항목:

- 이름
- 이메일
- 전화번호
- 회원 ID
- 가입일

추가 기능:

- 비밀번호 변경
- 로그아웃

비밀번호 변경 규칙:

- 새 비밀번호 2회 입력
- 두 값이 같을 때만 변경 가능
- 비밀번호 보기/숨기기 지원

## 7. 공연장 테블릿 기능 설계

## 7.1 QR스캔

목적:

- 이용자가 제시한 티켓 QR을 확인하여 입장 가능성을 1차 검증

현재 구조:

- QR 값에는 `ticketId` 기반 값 사용
- 스캔 결과를 통해 티켓 정보 조회 가능

## 7.2 얼굴 인증

목적:

- QR 검증 후 실제 소지자와 얼굴 등록 정보 일치 여부 확인

현재 화면 구성:

- 얼굴 인증 탭 진입
- 카메라 기반 얼굴 확인
- QR 또는 선택된 티켓 기준 얼굴 검증 가능

현재 구현은 목업/프론트 중심 구조이며, 향후 서버 연동으로 고도화 가능하다.

## 7.3 프로필

공연장 테블릿 프로필 기능:

- 현재 로그인 이메일 확인
- 비밀번호 변경
- 로그아웃

## 8. 관리자 기능 설계

## 8.1 회원목록

관리자는 현재 저장된 이용자 계정을 확인할 수 있다.

표시 대상:

- 회원가입을 통해 생성된 이용자 계정
- 비밀번호가 있는 계정 중심

표시 정보 예시:

- 이름
- 이메일
- 전화번호
- 가입일

## 8.2 공연등록

### 목적

공연장 운영을 위한 공연 회차 정보를 등록한다.

### 입력 항목

- 공연명
- 가수명
- 공연일
- 1회차 시간
- 2회차 시간
- 좌석수

각 회차는 별도 `Performance`로 생성된다.

### 자동 생성

회차별로 서로 다른 `공연코드`를 생성한다.

공연코드 규칙:

- 6자리
- 영문 대문자
- 회차별 고유값

### 등록 후 관리 기능

등록된 공연별로 다음 기능 제공:

- 복사
- 수정
- 삭제
- 상태 변경

상태 값:

- 운영
- 보류
- 종료

상태는 등록 폼이 아니라 `등록된 공연 목록`에서만 변경한다.

삭제 시:

- 커스텀 팝업 표시
- 문구: `정말 삭제 하겠습니까?`

## 8.3 티겟등록

현재 생성된 티켓 목록을 관리한다.

기능:

- 구매된 티켓 리스트 조회
- 티켓 삭제

삭제 시:

- 커스텀 팝업 표시
- 문구: `정말 삭제하시겠습니까?`

티켓 삭제 영향:

- 티켓 제거
- 전송 이력 제거
- 입장 로그 제거
- 얼굴 캡처 제거

## 8.4 입장로그

입장 처리 결과 및 검증 이력을 관리한다.

기록 항목:

- 티켓 ID
- 소지자 ID
- QR 유효 여부
- ZK 유효 여부
- 얼굴 유사도
- 결과(`allowed`, `manual_review`, `denied`)
- 생성일시

## 8.5 관리자 프로필

기능:

- 현재 로그인 이메일 확인
- 비밀번호 변경
- 로그아웃

## 9. 데이터 모델 설계

## 9.1 User

- `id`
- `name`
- `email`
- `phone`
- `password`
- `createdAt`

## 9.2 FaceProfile

- `id`
- `userId`
- `snapshotBlobIds: string[]`
- `status`
- `createdAt`

## 9.3 Performance

- `id`
- `eventName`
- `eventDate`
- `roundLabel`
- `shareCode` 현재 UI 문구는 `공연코드`
- `status`
- `seatCount`
- `artistName`
- `createdAt`

## 9.4 Ticket

- `id`
- `performanceId`
- `eventName`
- `eventDate`
- `seatNo`
- `buyerId`
- `holderUserId`
- `status`
- `createdAt`

## 9.5 TransferHistory

- `id`
- `ticketId`
- `fromHolderUserId`
- `toHolderUserId`
- `changedAt`

## 9.6 EntryLog

- `id`
- `ticketId`
- `holderUserId`
- `qrValid`
- `zkValid`
- `faceSimilarity`
- `result`
- `createdAt`

## 9.7 StoredFaceCapture

- `id`
- `ticketId`
- `holderUserId`
- `blobId`
- `createdAt`

## 9.8 ZkIdentity

- `id`
- `userId`
- `commitment`
- `mockSecretStoredLocal`
- `createdAt`

## 10. 저장소 구조

현재 `localStorage` 키는 다음과 같다.

- `gtf_users`
- `gtf_face_profiles`
- `gtf_performances`
- `gtf_tickets`
- `gtf_transfer_history`
- `gtf_zk_identities`
- `gtf_entry_logs`
- `gtf_face_captures`

세션 키:

- `gtf_session_user`
- `gtf_session_venue_tablet`
- `gtf_session_tablet_admin`

고정 계정 비밀번호 키:

- `gtf_credential_venue_tablet`
- `gtf_credential_tablet_admin`

## 11. UI/UX 원칙

- 홈 화면은 설명을 최소화하고 메뉴 중심으로 구성
- 상단은 단순하게 현재 역할, 버전, Home만 표시
- 로그인 전에는 하단 탭을 노출하지 않음
- 역할 진입 후에는 해당 역할에 필요한 탭만 노출
- 얼굴등록은 버튼 접근성이 높도록 카메라 프레임과 안내를 밀접 배치
- 음성 안내는 화면 텍스트로도 표시해 무음 환경을 지원
- 위험 동작(삭제, 취소)은 커스텀 팝업으로 재확인

## 12. 현재 기술 스택 및 구조

- 프레임워크: Next.js App Router
- 언어: TypeScript
- UI: React 컴포넌트 기반
- 저장소: 브라우저 `localStorage`
- 얼굴 랜드마크: `@mediapipe/tasks-vision`
- QR 생성: `react-qr-code`

## 13. 제약사항

현재 구조의 제약:

- 서버 DB가 없어서 브라우저 단위 데이터만 유지
- 일반 창과 시크릿 창은 데이터가 서로 공유되지 않음
- 얼굴 인증은 프론트 중심의 시뮬레이션/목업 성격이 일부 포함됨
- 공연장 테블릿과 관리자 계정은 현재 고정 계정 기반
- 입장 검증 로직은 실제 서버 검증/암호학 검증 전 단계

## 14. 향후 확장 제안

### 우선순위 높은 항목

- 서버 API 및 DB 연동
- 관리자에서 공연장 테블릿 계정 발급/회수
- 티켓 상태 이력 관리
- 입장 처리 실시간 반영
- 얼굴 등록/비교 백엔드 검증 연동

### 중장기 확장

- 공연 다회차 고급 편집
- 좌석 구역/등급 관리
- 티켓 양도 승인 정책
- 운영자 권한 레벨 분리
- 감사 로그/관리자 행동 이력

## 15. 결론

현재 GrabTicket은 역할 기반 운영 구조, 이용자 회원가입 및 로그인, 얼굴 등록, 얼굴 등록 기반 티켓 구매, 공연 등록, 티켓 및 입장 로그 관리까지 한 흐름으로 연결된 상태다.

프론트엔드만으로도 서비스 흐름 검증이 가능하도록 설계되어 있으며, 이후 서버/DB/실제 인증 시스템을 붙일 때도 확장 가능한 구조를 유지하고 있다.
